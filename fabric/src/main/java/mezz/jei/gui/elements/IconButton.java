package mezz.jei.gui.elements;

import net.minecraft.client.gui.GuiGraphics;

/**
 * 编译期 stub（headless JEI 独立化后 BRBE 不再内嵌 mezz.jei.*）：
 * 真实 JEI 存在时其同名类在运行时遮蔽此 stub（类加载顺序 jei < brbe）。
 * 本 stub 仅供 fabric mixins（JeiIngredientListOverlayMixin 等）编译。
 */
public class IconButton {
    public void draw(GuiGraphics gui, int x, int y, float delta) {
    }
}
