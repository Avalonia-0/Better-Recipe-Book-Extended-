# 无头 JEI 独立化迁移方案

## 目标
把各核心分支（1.21.1 / 1.21.11 / 26.2）内嵌的无头 JEI 源码迁入独立项目
`headless-jei/{GAME_VERSION}`（本分支，独立 Gradle 工程），核心分支删除内嵌源码，
改为**依赖独立项目产物**（玩家同时安装 headless-jei mod + BRBE mod）。

## 独立项目内容（每版本）
- `mezz.jei.*` fork（官方源码内嵌：1.21.1=CommonApi+Common+Library 605 文件；
  1.21.11=854；26.2=841）
- `com.alonie.brbe.jei.*`：无头核心（BrbeJeiHeadlessCore/JEIPluginFinder/键位 stub/
  连接 stub）+ 收集（loader/stub/data-only engine）+ 索引（PluginRecipeIndexer）
- 平台实现（1.21.1：mezz.jei.fabric / mezz.jei.neoforge + ServiceLoader + AW/AT；
  1.21.11/26.2：mezz.jei.fabric 平台辅助 + JeiGuiSpriteManager 等）
- 资源 `assets/jei`（GUI 贴图/图集/lang）与 mod 清单（fabric.mod.json /
  neoforge.mods.toml，id `headlessjei`）
- **轻量桥** `com.alonie.brbe.jei.api`：
  - `JeiRecipeRegistry`：`typeUid → List<Entry>`（Entry = recipe 对象 + inputs/outputs +
    槽位布局 + 尺寸；值对象，零 mezz 类型泄漏）
  - `JeiPopupRenderer`：把 Entry 渲染为完整 JEI UI（createRecipeLayoutDrawable 缩放 +
    弱缓存 + 20Hz tick）

## 消费者（BRBE 核心分支）改动计划
1. 删除内嵌源码：`mezz/jei/**`、`com/alonie/brbe/jei/**`、fabric/neoforge 的
   `mezz/**` 平台实现与接线、`assets/jei`、JEI 相关 AW/AT 条目（恢复最小集）；
   保留 BRBE 侧"引擎/UI 适配"（RecipeViewerEngine.JeiEntry/registerJeiType、
   queryJei、PopupRenderer 代理、DisplayEntry、SyntheticRecipeRendererImpl 等）
2. 编译依赖：`compileOnly files("libs/headless-jei-<mc>-<ver>.jar")`
   （libs/ 放独立项目构建产物；mezz.jei.api 类由该 jar 提供）
3. 运行时：玩家 mods 目录同时装 headless-jei（无 JEI 场景）或真实 JEI（共存场景，
   类加载遮蔽 jei < zzzbrbe 不变）
4. 数据接线：JOIN/重建时从 `JeiRecipeRegistry` 拉条目转进 RecipeViewerEngine
   （新 `BrbeJeiBridge` 或并入现有 RecipeViewerIndex 重建钩子）

## 构建环境问题（2026-08-28 记录）
新工程冷配置在 1.21.1（architectury + neoforge 平台）下偶发
`Could not find method neoForge()` / `Failed to compute checksum`（核心分支因老
`.gradle` 配置缓存正常）。fabric-loom-remap 冷管线（1.21.11）验证可用。
排查建议：
- `JAVA_HOME=...gradlew --stop` 重启 daemon
- `rm -rf ~/.gradle/caches/fabric-loom`（971M，会重下）
- 或者用核心分支已生成的项目级 `.gradle/loom-cache` 复制到新工程
